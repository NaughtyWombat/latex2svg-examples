\begin{theorem}
If a square matrix has linearly independent columns, then it is invertible.
\end{theorem}

\begin{proof}
The columns form a basis, so the associated linear map is one-to-one and onto.
Therefore the matrix has an inverse.
\end{proof}
